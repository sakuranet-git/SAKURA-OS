# SAKURA OS リリースノート

## v1.9.3 (2026-06-09)
### 修正
- **SAKURA BLOOM URL修正**: URLを `/system/SAKURA-BLOOM/` → `/system/SAKURA-BLOOM/index.php` に変更。トレイリングスラッシュだと静的LP（index.html）が表示され管理画面に入れなかったため、ポータルと同じ `index.php`（管理画面）を直接指定。
### 保護
- 変更前バックアップを `backups/v1.9.3_bloom_url_fix_20260609/` に作成。

## v1.9.2 (2026-06-09)
### 追加
- **SAKURA BLOOMアプリ追加**: アプリ一覧に「SAKURA BLOOM（🌸）」を追加。請求書の直下に配置。クリックすると `/system/SAKURA-BLOOM/` をOS内ウィンドウで開く。
### 保護
- 変更前バックアップを `backups/v1.9.2_bloom_app_20260609/` に作成。
- 既存アプリ設定は変更なし。

## v1.9.1 (2026-06-07)
### 追加
- **SAKURA Mailアプリ追加**: 左パネルのアプリ一覧に「メール」を追加。クリックすると `/system/mail.php` をOS内ウィンドウで開く。
### 保護
- 変更前バックアップを `backups/v1.9.1_webmail_app_20260607/` に作成。

## v1.9.0 (2026-05-15)
### 追加
- **名刺管理アプリ追加**: アプリ一覧に「名刺管理（🪪）」を追加。顧客管理の直下に配置。クリックすると `/system/cards.php` をOS内ウィンドウで開く。
### 保護
- 変更前バックアップを `backups/v1.9.0_card_app_20260515/` に作成。
- 既存アプリ設定は変更なし。

## v1.8.9 (2026-05-11)
### 追加
- **勤怠管理アプリ追加**: 左パネルのアプリ一覧に「勤怠管理」を追加。クリックすると `/system/timecard.php` をOS内ウィンドウで開く。
### 保護
- 変更前バックアップを `backups/v1.8.9_timecard_app_20260511/` に作成。
- 既存アプリ、壁紙、ニュース、グループウェア、請求書の設定は変更なし。

## v1.8.7 (2026-03-28)
### バグ修正
- **壁紙・スライドショーの初期化順序を修正**：`fetchServerWallpapers` を `init()` で必ず `await` するよう変更。スライドショー設定の読み込みより先にサーバー画像リストを取得することで、タイミング問題を解消
- プリセット壁紙設定時もサーバー画像リストを取得するよう変更（スライドショーで使用可能に）

## v1.8.6 (2026-03-28)
### データ保護強化
- **付箋メモを IndexedDB に移行**：`localStorage.clear()` では消えなくなった。旧 `sakura_sticky_notes` からの自動移行対応
- **スライドショー設定を IndexedDB に移行**：ON/OFF・間隔設定が `localStorage.clear()` で消えなくなった。旧 `sakura_os_slide` からの自動移行対応
- **スライドショーをサーバー壁紙でも動作可能に**：アップロード画像がない場合はサーバー壁紙（165枚）でスライドショーが動くよう改善。停止時はサーバー壁紙タイマーを5分サイクルに戻す

## v1.8.5 (2026-03-28)
### バグ修正
- v1.8.4で修正しきれなかった壁紙問題を完全修正：`sakura_os_wp` にユーザー明示フラグ（`x:1`）を追加。フラグなし（バグ自動保存）の場合はキーを削除してサーバー壁紙フォールバックへ正しく遷移するよう変更

## v1.8.4 (2026-03-28)
### バグ修正
- アップロード壁紙参照が残っているがIndexedDBが空の場合、グラデーションが永続保存されてサーバー壁紙が呼ばれなくなる問題を修正（staleキーを削除してサーバー壁紙フォールバックに正しく流れるよう変更）

## v1.8.3 (2026-03-28)
### 新機能
- **マスコット 木之本桜ちゃん追加**: 🔄ボタンで花帆ちゃん → みこちゃん → 桜ちゃんの3キャラ切り替えに対応
  - 動画: `/system/sakura-ai/assets/mascot-sakura-video.mp4`
  - セリフ: ほえええ～！/ はにゃ～ん…/ 絶対だいじょうぶだよ/ 封印解除（レリーズ）！ 等8種
  - 文字色: `#ff69b4`（ホットピンク）
- **サーバー壁紙フォールバック追加**: localStorage/IndexedDBが空の場合、SAKURA AIの `wallpapers/`（165枚）を自動で壁紙として表示（5分ごとスライドショー）
  - ブラウザストレージがクリアされても壁紙が消えなくなった
### バグ修正
- `localStorage.clear()` がSAKURA OSの壁紙設定を巻き込む問題を修正（SAKURA AI専用キーのみ削除に変更）

## v1.8.2 (2026-03-23)
### 新機能
- **マスコットキャラ切り替え機能**: 花帆ちゃん ⇔ さくらみこちゃんを🔄ボタンでトグル切り替え
  - みこちゃん専用セリフ（にゃっはろー！/ みこだわ！等）
  - みこちゃん動画: `/system/sakura-ai/assets/mascot-miko-video.mp4`
  - キャラ選択状態を `sakura_os_mascot_char` に localStorage 保存

---

## v1.8.1 (2026-03-23)
### 新機能
- **マスコット花帆ちゃん追加**: タスクバーの🎬ボタンでON/OFF（デフォルトOFF）
- 吹き出し付き・6秒ごとにセリフローテーション・動画クリックで切り替え
- 吹き出し方向を右・下・左・上の4方向で切り替え可能
- ドラッグ移動・位置/方向をlocalStorageに保存

---

## v1.8.0 (2026-03-23)

### SAKURA AI 連携 Phase 2: AI から顧客を検索する

- SAKURA AI から「田中さんを検索して」などと話しかけると、顧客管理アプリが開き自動で検索される
- `search_customer` ツール追加（chat_stream.php / agent_stream.php 両対応）
- postMessage 経由で ordersystem.html の検索ボックスに自動入力 → `filterTable()` 実行
- ordersystem.html に SAKURA OS 連携リスナー追加
- SAKURA AI 側の変更は sakura-ai リポジトリ v0.57 参照

## v1.7.0 (2026-03-22)

### SAKURA AI 連携 Phase 1: AI からアプリを開く

- SAKURA AI（エージェントモード）から `open_app` ツールを呼び出すと、SAKURA OS 上のアプリが自動的に開くようになった
- postMessage 双方向通信（AI iframe → SAKURA OS → openApp()）を実装
- 対応アプリ: 顧客管理・申込み管理・利用明細・通話明細・SAKURA AI・SAKURA MUSIC・グループウェア・請求書
- SAKURA AI 側の変更は sakura-ai リポジトリ v0.56 参照

## v1.6.1 (2026-03-22)

### マスコット機能を完全削除

- SAKURA-NET-Mascot 連携を削除（CSS link / JS script / トグルUI / JS関数）
- `SAKURA-NET-Mascot/` フォルダをローカルから削除
- 他の機能（アプリ、天気、ニュース、カレンダー、付箋メモ、AIパネル等）には一切変更なし

## v1.6.0 (2026-03-21)

### アプリ追加：請求書

- APPS 配列に請求書（🧾）を追加
- URL: `/system/invoice/invoice.html`（w:1200 / h:760）

## v1.5.9 (2026-03-16)

### パフォーマンス最適化

- クロック更新（毎秒）の DOM クエリをキャッシュ化：`getElementById` × 4 を setInterval 外に移動
- Firestore `onSnapshot` リスナーを `beforeunload` で確実に解除（メモリリーク修正）
- 重複していた `DOMContentLoaded` ハンドラを1つに統合

## v1.5.8 (2026-03-16)

### UI改善：アプリアイコン Mac スタイル化

- 左パネルのアプリ一覧をリスト → 3列グリッドに変更
- 各アプリに個別カラーのグラデーション丸角アイコン（52px・光沢レイヤー付き）
- アイコン下にラベルを配置するデスクトップアイコン型レイアウト

## v1.5.7 (2026-03-16)

### マスコット最適化

- PNG が透明背景済みのため Canvas の白背景除去ループを削除（`clearRect` + `drawImage` のみに簡略化）

## v1.5.6 (2026-03-16)

### マスコット最適化（Gemini更新対応）

- マスコットサイズを 200px → 300px に拡大
- 吹き出し位置調整（`bottom: 310px`、`left: 80px`）
- `_initPosition()` から `_loadPosition()` 呼び出しが削除されていたバグを修正（ドラッグ位置がリロードで消える問題）

## v1.5.5 (2026-03-16)

### マスコット最適化（Gemini更新対応）

- マスコットサイズを 110px → 200px に拡大（canvas インラインサイズを修正）
- 吹き出し位置を新サイズに合わせて調整（`bottom: 210px`、`left: 60px`）
- 吹き出しの show/hide アニメーション不整合を修正（`translateX(-50%)` を削除）

## v1.5.4 (2026-03-16)

### マスコット背景完全透明化

- `mix-blend-mode` から Canvas API 方式に変更：白・淡色ピクセルを直接 alpha=0 に変換
- アンチエイリアス境界もグラデーション処理で自然に透明化

## v1.5.3 (2026-03-16)

### マスコット修正

- ドラッグ機能の呼び出し漏れを修正（`_initDrag()` が未実行だった）
- 背景透明化：ダークモードの `mix-blend-mode: screen` を削除し `multiply` に統一（白背景を正しく除去）

## v1.5.2 (2026-03-16)

### マスコット再修正

- ON/OFFトグルを壁紙パネル内から左パネル（常時表示）に移動
- カレンダー下への配置：window.load後にレイアウト完了を待って位置計算
- ドラッグ位置の保存・復元（localStorage）

## v1.5.1 (2026-03-16)

### マスコット改善

- 配置をカレンダー・本日の作業予定の下にデフォルト変更
- マスコット画像の白背景を透明化（`mix-blend-mode` 利用）
- ドラッグで好きな場所に移動可能・位置を localStorage に保存

## v1.5.0 (2026-03-16)

### 新機能：マスコットキャラクター追加

- SAKURA OS マスコットをデスクトップ左下に常駐表示
- ふんわり浮遊アニメーション、ホバーで反応、クリックでジャンプ＆ランダムメッセージ表示
- 設定パネル（壁紙変更）にマスコット表示 ON/OFF トグルスイッチ追加
- ON/OFF 設定は localStorage に永続保存（リロード後も維持）

## v1.4.2 (2026-03-16)

### UI改善

- 枠線を `2px` + 不透明度 `0.55〜0.60` に強化、`--panel-border` も `0.55` に引き上げ
- `--text-dim` を完全不透明・`--text-muted` を `0.78` に引き上げ
- 全テキストに `text-shadow` を body 基底として適用、主要テキストはさらに強化

## v1.4.1 (2026-03-16)

### UI改善

- 全パネルの `backdrop-filter:blur` を完全削除→クリアな表示に
- 全パネル背景を `transparent` に変更、枠線を `1.5px` に強調

## v1.4.0 (2026-03-16)

### UI改善

- ダークモード：全パネル（ニュース・為替・カレンダー・タスク・左パネル）を超透明化（壁紙がしっかり透過）
- ダークモード：全枠線を明るく強調（`rgba(255,255,255,0.30~0.35)`）
- ダークモード：主要テキストに `text-shadow` 追加し文字を見やすく
- SAKURA OS ロゴクリックでページ再読み込み（トップに戻る）機能追加

## v1.3.4 (2026-03-16)

### バグ修正

- カレンダー・本日の作業予定が反映されない問題を根本修正：`gw_api.php` 経由の設定取得・匿名認証を廃止し、`ordersystem.html` と同一方式（Firebase設定ハードコード・認証不要）に変更

## v1.3.3 (2026-03-16)

### バグ修正

- カレンダー・本日の作業予定が反映されない問題を修正：`gw_api.php` の相対パスを `groupware/gw_api.php` に修正（`/system/gw_api.php` ではなく `/system/groupware/gw_api.php` が正しいパス）

## v1.3.2 (2026-03-16)

### バグ修正

- 壁紙アップロードをIndexedDBで保存する方式に変更（グループウェアと同方式・LocalStorageの5MB制限を完全回避）
- 旧LocalStorage保存データからIndexedDBへの自動移行対応

## v1.3.1 (2026-03-16)

### バグ修正

- カレンダー・本日の作業予定が最新データに反映されない問題を修正：Firebase Auth SDKを追加し匿名サインインでFirestoreのセキュリティルールを通過するように対応

## v1.3.0 (2026-03-16)

### バグ修正・改善

- パネル・カード透明度を大幅強化（壁紙がしっかり透けて見えるレベルに）
- ニュース9枚目：取得件数が9未満の場合もループ表示して空欄なし

## v1.2.0 (2026-03-16)

### バグ修正・機能追加

- 壁紙ボタン修正：pointer-events:none で非表示時のクリックブロック問題を根本解決
- ニュースソースを Yahoo! ニュース（rss2json API）に変更、NHK をフォールバックに
- ニュース12件表示保証（取得件数が不足の場合は繰り返し表示）
- 壁紙アップロード機能：画像ファイルを base64 変換して localStorage 永続保存（バージョンアップ後も維持）

## v1.1.0 (2026-03-16)

### ダッシュボードUI全面リニューアル

- 左パネル追加：大型デジタル時計（秒表示）・日付・令和/干支・📍大阪市
- リアルタイム天気ウィジェット（wttr.in API / 大阪市）：気温・体感・湿度・風速・降水・3日間予報
- 上部ニュースティッカー：NHK RSSライブ取得（allorigins プロキシ経由）
- デスクトップに為替レートカード（USD/JPY・EUR/JPY・CNY/JPY）
- デスクトップにニュースグリッド（6件表示）
- 壁紙変更機能：プリセット9種 + カスタムURL入力（localStorage保存）
- アプリアイコンを左パネルに移動

## v1.0.0 (2026-03-16)

### 初回リリース

- デスクトップOS風ランチャー画面を新規作成
- 7アプリをiframeウィンドウで起動（顧客管理・申込み管理・利用明細・通話明細・SAKURA AI・SAKURA MUSIC・グループウェア）
- ドラッグ＆ドロップによるウィンドウ移動
- 右下ドラッグによるウィンドウリサイズ
- 最小化（タスクバーへ格納）・最大化・閉じる
- 複数ウィンドウの前面/背面管理
- 下部タスクバーで開いているウィンドウを一覧表示
- ダーク/ライトテーマ切り替え（localStorage保存）
- PHP セッション認証（index.php と共通）
- 時計表示（タスクバー右側）
